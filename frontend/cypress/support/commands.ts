// cypress/support/commands.ts

// Custom commands for Cypress tests

// Example:
// Cypress.Commands.add('login', (email, password) => { ... })

// For better TypeScript support, add command declarations to cypress.d.ts

export {};
